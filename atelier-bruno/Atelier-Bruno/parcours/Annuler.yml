info:
  name: Annuler
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/bookings/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
