info:
  name: Payer
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/bookings/{id}/payment"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
