info:
  name: Relire sa réservation
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/bookings/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
