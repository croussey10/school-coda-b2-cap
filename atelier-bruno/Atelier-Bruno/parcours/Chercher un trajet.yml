info:
  name: Chercher un trajet
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/trips"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
